[pytest]
; ignore scripts for generating test data (--ignore tests/data/*.py)
; run coverage (--cov .)
; report uncovered lines (--cov-report term-missing)
testpaths = tests
addopts =  --ignore tests/data --cov . --cov-report term-missing

